import {Region} from "./region";

export interface ProfileInfoJSON {
  alias: string;
  region: string;
  pronouns: string;
  bio: string;
  password: string;
  email: string;
  phoneNumber: string;
  ssn: string;
  profilePic: string;
}
